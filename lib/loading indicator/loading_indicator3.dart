import 'package:flutter/material.dart';

class LoadingIndicator3 extends StatelessWidget {
  const LoadingIndicator3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const LinearProgressIndicator(color: Color(0xFF686D76));
  }
}
